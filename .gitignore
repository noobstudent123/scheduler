# validation harness — never commit installed deps, build temp, or local data
tools/node_modules/
tools/.probe-tmp/
tools/package-lock.json

# real course data stays local (never committed) — scoped to real data only,
# so sample/fixture spreadsheets (e.g. tools/fixtures/*.xlsx) stay committable.
Mandakh_Course_Data*.xlsx
*_real*.xlsx
